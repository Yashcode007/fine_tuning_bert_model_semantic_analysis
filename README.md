# fine_tuning_bert_model_semantic_analysis
Fine-tune the BERT by adding a few neural network layers on our own and freezing the actual layers of BERT architecture. I would be of classifying sentences into POSITIVE and NEGATIVE by using fine-tuned BERT model
